# 师座（TeacherDeck）· AI 教师工作台 — 产品需求文档（PRD）

> 版本：v0.3（产品名已定：师座；方案 A 已纳入：成绩分析 + 评语增强）
> 日期：2026-08-08
> 状态：待评审
> 定位：教师自己的 Codex 工作台（BYOK，客户自带 api.sublyx.org Key，无自建支付）

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与痛点](#2-目标用户与痛点)
3. [竞品分析](#3-竞品分析)
4. [需求切入：场景筛选与优先级](#4-需求切入场景筛选与优先级)
5. [MVP 范围](#5-mvp-范围)
5A. [方案 A 落地：新增成绩分析场景 + 评语增强](#5a-方案-a-落地新增成绩分析场景--评语增强)
6. [商业模式：客户自带 Key（BYOK）](#6-商业模式客户自带-keybyok)
7. [技术方案](#7-技术方案)
8. [实测验证记录](#8-实测验证记录)
9. [数据模型（演进路线）](#9-数据模型演进路线)
10. [里程碑与路线图](#10-里程碑与路线图)
11. [风险与开放问题](#11-风险与开放问题)
12. [附录：现有代码资产](#12-附录现有代码资产)
13. [产品名候选：教师自己的 Codex 工作台](#13-产品名候选教师自己的-codex-工作台)

---

## 1. 产品概述

### 1.1 背景

对标站点 OurTeacher（ourteacher.cc）定位为"专注教师 AI 场景，幼、小、初、高、职、大学全学段覆盖"的**大而全平台**。其弱点是：覆盖面广但每个场景深度有限、收费模式不清晰、与免费通用 AI（豆包/Kimi/文心一言）正面竞争。

本产品放弃"大而全"路线，**只做教师最高频、最痛、且通用 AI 做不好的几个刚需场景**，用「专业场景提示词 + 客户自带中转站 Key」的方式快速落地。

### 1.2 产品定位

> **一句话**：一个让教师"填上自己的 API Key 就能用"的 AI 工作台，专注 6 个刚需场景（作文批改 / 期末评语 / 出题组卷 / 教学反思 / 家校沟通 / 成绩分析）。

- **对客户**：不用学提示词，选场景、填内容、拿结果；自带 Key，余额透明，无订阅绑定。
- **对开发者（本项目）**：不做支付、不做用户体系（MVP）、不做模型上游，只做「壳 + 场景 + 体验」，成本极低、可快速验证。

### 1.3 产品原则

| 原则 | 说明 |
|------|------|
| 场景为王 | 每个场景 = 一个专业系统提示词，这是产品壁垒（通用 AI 做不好） |
| 无自建支付 | 余额/扣费/账单全部由中转站处理，客户自带 Key |
| 结果可验证 | 输出可直接修改使用，如批改评分、评语、试卷 |
| 安全边界 | 不暴露 bash / 文件系统写操作（除成果保存目录） |

---

## 2. 目标用户与痛点

### 2.1 用户画像

| 画像 | 描述 | 付费能力 |
|------|------|---------|
| 一线学科教师（主） | 语文/数学/英语/理化教师，每周批改、备课、写材料 | 个人小额付费 |
| 班主任（主） | 除教学外承担评语、家长会、家校沟通、总结材料 | 个人小额付费 |
| 教研员/备课组长 | 出题组卷、命制测试 | 可向上采购 |
| 培训机构教师 | 需求同公立校，付费意愿更强 | 个人/机构付费 |

### 2.2 痛点分析（按强度排序）

| 痛点 | 频次 | 强度 | 说明 |
|------|------|------|------|
| 作业/作文批改耗时 | 每周 | ★★★★★ | 批改 40-50 人作业是最大时间黑洞；作文批改需要逐段点评，最痛苦 |
| 期末评语批量编写 | 每学期 | ★★★★ | 40-50 人/班，每人要差异化、真诚、不雷同，纯苦力 |
| 出题组卷 | 每月 | ★★★★ | 需贴合课标知识点、难度分布、附答案，工作量大 |
| 教学总结/反思 | 每月/期末 | ★★★ | 不得不写、写了没人看，AI 替代感最强 |
| 家校沟通话术 | 每周 | ★★★ | 怕说错话、怕被截图，需要得体话术 |
| 教案/课件 | 每周 | ★★★ | **已被免费通用 AI 覆盖，不纳入本产品** |

---

## 3. 竞品分析

### 3.1 OurTeacher（ourteacher.cc）— 对标但不模仿

- 定位：全学段教师 AI 平台，含聊天、下载客户端、任务场景。
- 弱点：① 大而全导致单场景浅；② 免费 AI 可替代其多数功能；③ 无清晰按量计费，靠平台订阅。
- 启示：**做深不做广**，聚焦 1-3 个"通用 AI 做不好"的场景。

### 3.2 免费通用 AI（豆包 / Kimi / 文心一言 / DeepSeek 网页版）

- 能做的：教案、PPT 大纲、泛泛的作文评语、通用材料。
- 做不好的：① **作文拍照批改**（需视觉理解 + 分维度评分 + 逐段批注的结构化输出）；② **批量差异化评语**（40-50 人不雷同）；③ **贴合课标的组卷**（知识点标注 + 难度 6:3:1 + 评分标准）。
- 结论：本产品的价值 = **结构化专业输出**，不是"能生成"。

### 3.3 付费竞品

- 作业帮/好分数：偏题库与家校，批改以客观题为主，主观题/作文批改体验一般。
- 各类"作文批改"小程序：质量参差，多为简单评分。
- 机会：用"自带 Key"模式提供**低成本、高质量、透明计费**的替代选择。

---

## 4. 需求切入：场景筛选与优先级

### 4.1 筛选标准

每个候选场景按 5 个维度打分：**频次、痛点强度、通用 AI 可替代性（越低越好）、token 消耗（越高毛利越高）、付费意愿**。

### 4.2 场景矩阵

| 场景 | 频次 | 痛点 | 通用AI可替代 | token消耗 | 付费意愿 | 结论 |
|------|------|------|------------|-----------|---------|------|
| **作文批改** | 每周 | ★★★★★ | 低（需视觉+结构化） | 高（图+长文） | ★★★★★ | ✅ 杀手锏 |
| **期末评语** | 每学期 | ★★★★ | 中（批量+差异化难） | 高（批量） | ★★★★ | ✅ 高频刚需 |
| **出题组卷** | 每月 | ★★★★ | 中（贴合课标难） | 中高 | ★★★★ | ✅ 刚需 |
| **教学反思/总结** | 每月/期末 | ★★★ | 中 | 中 | ★★★ | ✅ 易用钩子 |
| **家校沟通** | 每周 | ★★★ | 高 | 低 | ★★ | ✅ 获客钩子（低价高频） |
| **成绩分析** | 每月 | ★★★★ | 低（需解析+统计+报告） | 高（长报告） | ★★★★ | ✅ 新增（方案 A） |
| 教案生成 | 每周 | ★★★ | 高（免费工具已覆盖） | 中 | ★★ | ❌ 放弃 |
| PPT/课件 | 每周 | ★★★ | 高 | 中 | ★★ | ❌ 放弃 |
| 学情分析 | 每月 | ★★★ | 低（需数据上传+统计） | 高 | ★★★★ | ✅ 并入成绩分析场景（方案 A） |

### 4.3 MVP 场景定义（6 个）

每个场景 = 一个**专业系统提示词**（详见 `lib/scenarios.ts`）：

| ID | 场景 | 核心输出 |
|----|------|---------|
| `essay` | 作文批改 | 分维度评分（内容/结构/语言/书写）+ 综合评语 + 逐段批注 + 升格建议（含改写示范）；支持拍照上传 |
| `comment` | 期末评语 | 按学生信息批量生成 60-100 字差异化评语（性格/成绩/特长/改进点），同班不雷同 |
| `exam` | 出题组卷 | 课标知识点标注 + 难度 6:3:1 + 题型分布 + 参考答案与评分标准 |
| `reflection` | 教学反思/总结 | 亮点(具体到环节) / 不足与归因 / 改进措施(含验证) / 下一步计划 |
| `parent` | 家校沟通 | 群发通知 / 一对一话术，先肯定后建议，附发送提示 |
| `analysis` | 成绩分析 | 班级均分/优秀率/及格率、进退步名单、知识点失分统计、分层教学与培优补差建议；支持粘贴/上传成绩表 |

### 4.4 暂不做的（明确排除）

- 教案生成、PPT/课件（免费 AI 已覆盖，无付费理由）
- 题库系统（重资产，二期评估）
- 教务管理系统（学生档案 CRUD、课表、请假、座位、违纪统计等 17 个模块）——**明确不做**，见 §5A 决策记录
- 语音、客户端 App、小程序（先 Web 验证）

---

## 5A. 方案 A 落地：新增成绩分析场景 + 评语增强

> 本节为 2026-08-08 评审「教师一体化工作台」需求后的落地决策（方案 A）。
> 结论：**不整体集成教务管理系统**（19 个模块中 17 个为纯 CRUD，违背“精准切入”原则）；
> 只吸收其中 AI 价值密度最高的两项 —— **成绩分析** 与 **评语批量增强**。

### 5A.1 评审结论（为什么不整体集成）

| 维度 | 结论 |
|------|------|
| 定位 | “一体化工作台”本质是教务管理系统（数据密集 CRUD），与师座（AI 场景工具）是两种物种 |
| 工作量 | 19 模块 + Excel/Word/图片导出 + 备份 + 响应式 ≈ 3-5 周全职，违背 MVP 节奏 |
| 技术栈 | 需结构化数据层（学生/成绩/课表），师座当前无状态（Key 存浏览器） |
| 与 AI 的化学反应 | 19 模块中仅 **成绩分析**、**学生管理→评语** 两项 AI 价值高，其余 17 项纯 CRUD |
| 决策 | 只做方案 A：新增「成绩分析」场景 + 增强「期末评语」批量输入 |

### 5A.2 新场景：成绩分析（`analysis`）

**目标**：老师粘贴/上传成绩表 → 输出学情分析报告。这是“一体化工作台”里 AI 价值密度最高的模块，且**通用 AI 做不好**（需结构化解析 + 统计 + 报告）。

**输入**：
- 方式 1：粘贴文本（支持“姓名+各科/各题得分”表格或纯文本）
- 方式 2：上传 Excel/CSV（二期，MVP 先做文本粘贴 + 结构化解析）
- 附带信息：考试类型（单元测/月考/期中/期末/口算）、满分值、是否含上次成绩（用于进退步）

**输出**（Markdown 报告）：
1. **班级概况**：人数、均分、优秀率、及格率、最高/最低分、标准差
2. **分段分布**：优秀/良好/达标/待提高 各段人数与名单
3. **进退步分析**（若提供上次成绩）：进步/退步 Top N 名单及幅度
4. **知识点失分统计**：若成绩表按题号/知识点列 → 逐知识点得分率，标注薄弱点
5. **分层建议**：优生拔高、待提高学生补差的具体行动项
6. **培优补差名单**：可直接复制使用的名单

**系统提示词要点**：结构化解析先行、数字必须来自输入（不虚构）、按学段调整优秀/及格线、输出可直接使用的表格与名单。

### 5A.3 增强：期末评语（`comment`）批量输入

**现状**：已支持“粘贴多名学生信息”批量生成。
**增强**：
1. 前端提供**表格模板引导**（姓名 | 成绩表现 | 性格特点 | 特长 | 需改进点），老师可填一行一人
2. 支持“按成绩自动填充表现”快捷操作（如粘贴成绩后一键生成评语草稿）
3. 输入区增加“示例格式”折叠提示

### 5A.4 影响范围

| 位置 | 改动 |
|------|------|
| `lib/scenarios.ts` | 新增 `analysis` 场景 + `comment` 场景增强提示词（表格输入引导） |
| `app/billing-demo/page.tsx` | 场景列表加“成绩分析”；评语场景显示表格模板引导；输入区支持粘贴 |
| `app/api/llm/route.ts` | 无改动（场景由 `scenarios.ts` 驱动） |
| 前端解析 | 二期：`analysis` 支持上传 Excel/CSV（需引入解析库，MVP 先文本粘贴） |

### 5A.5 工作量估算

| 项 | 估算 |
|----|------|
| `analysis` 场景提示词 + 注册 | 0.5 天 |
| 前端场景入口 + 表格模板引导 | 0.5 天 |
| 文本粘贴解析（老师粘贴成绩表 → 规范化） | 0.5-1 天 |
| Excel/CSV 上传（二期） | 1 天 |
| 合计（MVP 部分） | **1.5-2 天** |

---

## 5. MVP 范围

### 5.1 功能范围（In Scope）

| 模块 | 功能 | 状态 |
|------|------|------|
| Key 管理 | 客户在页面填入 api.sublyx.org 的 Key，存浏览器 localStorage | ✅ 已实现 |
| 场景工作台 | 6 个场景选择 + 内容输入（文本）+ 一键调用 | ✅ 已实现（成绩分析待实现） |
| 作文拍照 | 图片上传（data URL）→ 多模态批改 | ✅ 已实现 |
| **附件解析** | 图片→视觉；Word(.docx)/PDF/Excel(.xlsx)/文本→服务端解析成内容喂模型 | ✅ 已实现 |
| **多格式导出** | 生成结果导出 Word(.docx)/Excel(.xlsx)/PDF/Markdown | ✅ 已实现 |
| Token 用量展示 | 每次调用显示输入/输出/总 token | ✅ 已实现 |
| 模型选择 | 保存 Key 后自动拉取中转站可用模型列表，下拉选择；调用时带上所选模型 | ✅ 已实现 |
| 本地用量记录 | 本次浏览器保存最近 50 条调用记录 | ✅ 已实现 |
| 打字机输出 | 非流式响应 + 前端打字机模拟流式 | ✅ 已实现 |
| 错误提示 | 无 Key / 中转站错误 / 无模型 的友好提示 | ✅ 已实现 |
| 成果保存（工作台） | 教案/试卷等保存到 artifacts 目录（教师工作台功能） | ✅ 已实现 |

### 5.2 非功能范围（Out of Scope，MVP 明确不做）

- ❌ 自建支付 / 充值 / 余额 / 账单（全部交给中转站）
- ❌ 用户登录注册 / 多用户账号
- ❌ 服务端 Key 存储（Key 仅存客户端浏览器）
- ❌ 真实流式输出（非流式 + 打字机替代）
- ❌ 数据库（无持久化用户数据）

### 5.3 用户流程（单场景）

```
打开页面 → 填入 Key（一次，浏览器记住）→ 选场景 → 填内容（文本或拍照）
→ 点调用 → 打字机输出 → 查看 token 用量 → （如需）复制/保存成果
```

---

## 6. 商业模式：客户自带 Key（BYOK）

### 6.1 价值链

```
客户（教师）
   │ ① 在 api.sublyx.org 注册/充值/创建 Key（客户自己的钱，透明可控）
   ▼
本产品（师座，Web 应用）
   │ ② 填 Key → ③ 选场景 → ④ 组装专业提示词 → ⑤ 透传中转站
   ▼
api.sublyx.org（自有中转站，new-api 类，OpenAI 兼容）
   │ ⑥ 模型调用 → ⑦ 扣客户余额 → ⑧ 返回 usage
   ▼
上游模型（DeepSeek / GPT / Claude …）
```

### 6.2 收入模型（本产品如何赚钱）

| 模式 | 说明 | 阶段 |
|------|------|------|
| 中转站利差 | 客户在中转站按官方价充值，本产品不参与差价（MVP） | MVP |
| 场景订阅费 | 5 个专业场景打包，月费（如 ¥9.9/月），Key 客户自备 | 二期 |
| 机构版/白标 | 学校/机构采购：场景定制 + 内网部署 | 三期 |

> **MVP 阶段的核心验证**：客户是否愿意"自带 Key + 付费场景"使用？若验证通过，再叠加订阅费；验证失败则调整。

### 6.3 为什么不做自建支付（决策依据）

1. 支付合规（微信/支付宝商户、营业执照、备案）成本高、周期长。
2. 中转站（new-api）本身具备 token 计费、余额、渠道管理能力，重复造轮子无意义。
3. MVP 只需验证「场景价值」，支付可以后置。

---

## 7. 技术方案

### 7.1 总体架构

```
┌─────────────────────────────────────────────┐
│ 浏览器（Next.js 前端）                        │
│  - 场景选择 / 内容输入 / 图片上传 / 打字机输出   │
│  - Key 存 localStorage                        │
└──────────────────┬──────────────────────────┘
                   │ POST /api/llm (SSE)
┌──────────────────▼──────────────────────────┐
│ Next.js 后端（Node.js）                      │
│  - lib/scenarios.ts    场景提示词（产品壁垒）    │
│  - lib/relay-session.ts pi SDK 会话封装        │
│    （动态注册中转站 provider + 客户 Key）       │
│  - lib/relay.ts        中转站直连（模型列表）    │
│  - lib/teacher-tools.ts 自定义 Pi 工具         │
│  - app/api/llm         生产调用端点            │
│  - app/api/chat        开发调试通道（Pi SDK）   │
└──────────────────┬──────────────────────────┘
                   │ pi AgentSession（思考 + 工具）
                   │ OpenAI 兼容 /v1/chat/completions（客户 Key）
┌──────────────────▼──────────────────────────┐
│ api.sublyx.org（new-api 类中转站）→ 上游模型   │
└─────────────────────────────────────────────┘
```

### 7.2 技术栈

| 层 | 技术 | 版本 | 说明 |
|----|------|------|------|
| 前端框架 | Next.js | 16.3.0 | App Router + Turbopack |
| UI | React + Tailwind（内联 CSS 变量） | 19.2.4 | 自绘工作台风格 |
| 图标 | lucide-react | 0.468 | — |
| 后端 | Node.js API Routes | — | `runtime = "nodejs"` |
| 模型调用 | 直连中转站 OpenAI 兼容接口 | — | 生产通道 |
| 开发调试 | @earendil-works/pi-coding-agent | 0.84.0 | `app/api/chat`（工具编排/开发用） |
| 参数校验 | typebox | 1.3.7 | 自定义工具 schema |

### 7.3 模块设计

| 模块 | 文件 | 职责 |
|------|------|------|
| 场景模板 | `lib/scenarios.ts` | 6 个场景的 label / description / systemPrompt |
| **pi SDK 会话** | `lib/relay-session.ts` | **生产通道核心**：动态注册中转站 provider（客户 Key）+ 创建 AgentSession + 思考 + 工具 + usage 提取 + 图片解析 |
| 中转站客户端 | `lib/relay.ts` | `RELAY_BASE`（内置）、`listModels()`（模型列表）、非流式 `chatCompletion()`（备用/开发） |
| 自定义工具 | `lib/teacher-tools.ts` | `curriculum_lookup`（课标查询）、`save_artifact`（保存成果） |
| 调用端点 | `app/api/llm/route.ts` | 校验入参 → 附件解析（multipart 上传）→ 场景提示词 + 图片 → pi Session → SSE 返回思考/文本/工具/usage |
| 导出端点 | `app/api/export/route.ts` | 生成结果导出 Word/Excel/PDF/Markdown |
| 附件解析库 | `lib/file-parse.ts` | 图片/Word/PDF/Excel/文本 → 可喂模型的内容 |
| 导出库 | `lib/export.ts` | Markdown → docx / xlsx / pdf / md（内置楷体字体） |
| 模型列表端点 | `app/api/models/route.ts` | 客户 key 透传 `/v1/models`，返回可用模型 + 默认模型 |
| 自带Key页面 | `app/billing-demo/page.tsx` | Key 输入（localStorage）+ 模型下拉 + 场景选择 + 输入/图片 + 打字机 + 思考/工具展示 + 用量 |
| 教师工作台 | `app/page.tsx` + `app/api/chat` | 开发调试通道：Pi AgentSession + 自定义工具（课标查询/保存成果） |
| 教学数据 | `lib/teacher-data.ts` | 学段/学科/年级/教材 + 任务模板 + 内置课标要点 |

### 7.4 API 设计

**POST /api/llm**（生产调用，客户自带 Key）

```jsonc
// 请求
{
  "scenario": "essay",            // essay|comment|exam|reflection|parent|analysis
  "message": "请批改这篇作文…",
  "imageDataUrl": "data:image/jpeg;base64,...",  // 可选，拍照
  "apiKey": "sk-xxx",              // 客户在中转站创建的 Key
  "model": "gpt-5.5"               // 客户从可用模型列表中选择（可选，默认 RELAY_MODEL）
}
```

```jsonc
// SSE 事件流
data: {"type":"status","label":"正在调用中转站（gpt-5.5）"}
data: {"type":"delta","text":"…"}        // 完整内容一次性输出（前端打字机）
data: {"type":"usage","promptTokens":1493,"completionTokens":877,"totalTokens":2370}
data: {"type":"done"}
```

**GET /api/models**（客户 Key 拉取可用模型，已实测可行）

```jsonc
// 请求头 Authorization: Bearer <客户Key>，后端透传中转站 /v1/models
// 响应
{ "models": ["gpt-5.5", "gpt-5.6", "gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.6-luna", ...] }
```

**POST /api/chat**（开发调试通道，Pi AgentSession，不用于生产）

**GET /api/artifacts**（成果保存/列表/删除，教师工作台）

### 7.5 关键技术决策（含实测依据）

| 决策 | 依据（实测） |
|------|-------------|
| **生产通道走 pi SDK** | 2026-08-08 架构升级：客户要求「有思考、能调用工具」。实测 pi AgentSession + 动态注册中转站 provider 全链路可用（思考 41-89 字符、工具调用、图片识别、usage 完整） |
| **每请求独立创建 Session** | 客户 Key/模型/工具是请求私有的，动态注册 provider + 独立 ModelRuntime/SettingsManager，互不污染（代价：初始化约 0.3-0.5s） |
| **注册模型必须带 cost** | 实测：缺 cost 时工具调用路径 `calculateCost` 访问 `model.cost.tiers` 崩溃（`Cannot read properties of undefined`）；cost 全 0 即可（本应用不计算费用） |
| **图片用 pi ImageContent** | pi 图片格式是 `{type:"image", data, mimeType}`（base64），不是 OpenAI 的 `image_url`；作文拍照需走 `session.prompt(msg, { images })` |
| **默认非流式调用（备用通道）** | 直连 `chatCompletion()` 保留作开发/备用；pi SDK 流式已由 pi 内部处理 usage |
| **Key 只存客户端** | 后端不落盘、只透传，降低 Key 泄露面；MVP 无用户体系 |
| **中转站地址内置** | `lib/relay.ts` 的 `RELAY_BASE` 常量，客户无需配置 |
| **不接余额接口** | 实测中转站 `/dashboard/billing/*`、`/api/user/self` 均不可用（返回 HTML/404），余额引导客户到中转站网页查看 |
| **模型由客户自选** | 实测 `/v1/models` 带 Key 返回 10 个可用模型（gpt-5.2 ~ gpt-5.6-luna），新增「模型选择」下拉，默认 RELAY_MODEL |
| **双通道并存** | `/api/llm`（生产：pi SDK）与 `/api/chat`（开发：Pi AgentSession 完整工具集）分离，职责清晰 |

### 7.6 安全边界

- ✅ 生产通道不暴露 bash / 文件写入 / 外部系统工具
- ✅ 场景提示词由服务端组装，客户无法注入工具
- ⚠️ Key 走前端 → 后端透传，存在被中间人截获风险（需 HTTPS）；后端不持久化 Key
- ⚠️ 图片 base64 会经过服务端，注意体积上限（建议 <5MB）

---

## 8. 实测验证记录（2026-08-08）

### 8.1 中转站探测

| 项 | 结果 |
|----|------|
| 中转站类型 | new-api 类（OpenAI 兼容），`/v1/models` 返回 `API_KEY_REQUIRED` |
| 模型列表 | gpt-5.5 / gpt-5.6-sol / gpt-5.6-terra / gpt-image-2（均支持图片输入，实测通过） |
| 余额接口 | ❌ 不可用（/dashboard/billing/* 返回 HTML，/api/user/self 404） |
| 流式 usage | ⚠️ 偶发缺失（带图时更明显） |
| 非流式 usage | ✅ 必定返回 |

### 8.2 端到端调用（真实 Key）

| 场景 | 输入 | 输出 | Token | 状态 |
|------|------|------|-------|------|
| 期末评语 | 2 名学生信息 | 2 条差异化评语 | 404（228+176） | ✅ |
| 作文批改（文本） | 作文正文 | 分维度评分+评语+批注+升格建议 | 2370（1493+877） | ✅ |
| 作文批改（拍照） | 生成的作文纸图片 | 同上，含书写卷面分 | 2370（1488+887） | ✅（流式，usage 偶发缺失） |
| 无 Key 调用 | — | 友好报错「请先填入…API Key」 | — | ✅ |

---

## 9. 数据模型（演进路线）

### 9.1 MVP：无数据库

- Key：浏览器 localStorage（`teacher-agent-api-key`）
- 用量记录：浏览器 localStorage（`teacher-agent-usage-log`，最近 50 条）
- 成果：本地 `artifacts/` 目录

### 9.2 二期（多用户 + 场景订阅）演进

```sql
-- 用户表
CREATE TABLE users (
  id         TEXT PRIMARY KEY,
  email      TEXT UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT now()
);

-- 用户绑定的中转站 Key（加密存储）
CREATE TABLE user_relay_keys (
  user_id    TEXT PRIMARY KEY REFERENCES users(id),
  key_enc    TEXT NOT NULL,   -- AES-GCM 加密
  updated_at TIMESTAMP
);

-- 订阅/额度（二期，若做场景订阅）
CREATE TABLE subscriptions (
  user_id    TEXT PRIMARY KEY REFERENCES users(id),
  plan       TEXT NOT NULL,   -- free|pro
  expires_at TIMESTAMP
);

-- 调用记录（用于对账与限流）
CREATE TABLE call_logs (
  id            BIGSERIAL PRIMARY KEY,
  user_id       TEXT REFERENCES users(id),
  scenario      TEXT NOT NULL,
  model         TEXT NOT NULL,
  prompt_tokens INT, completion_tokens INT, total_tokens INT,
  created_at    TIMESTAMP DEFAULT now()
);
```

> 说明：余额仍由中转站管理，本系统只存「绑定的 Key」与「调用记录」。

---

## 10. 里程碑与路线图

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| **M0 技术验证** | 中转站接入、usage 获取、视觉模型、端到端调用、模型列表拉取 | ✅ 完成 |
| **M1 MVP（当前）** | 自带 Key 页面 + 5 场景 + 拍照 + 用量展示 + 模型选择 | ✅ 完成（代码） |
| **M1.3 生产通道 pi SDK** | 生产 `/api/llm` 改走 pi SDK：思考 + 工具调用 + 图片识别，实测全链路通过 | ✅ 完成（2026-08-08） |
| **M1.5 方案 A** | 新增「成绩分析」场景 + 评语批量输入增强（表格模板引导）；Excel/CSV 解析留二期 | 🔨 待实现（详见 §5A） |
| **M2 验证迭代** | 真实教师试用 5-10 人；**小红书/公众号发布使用教程与场景内容**获客；收集反馈迭代提示词 | ⏳ 下一步 |
| **M3 多用户** | 登录注册 + Key 绑定（加密存服务端）+ 调用记录 | 待评估 |
| **M4 商业化** | 机构版/白标（订阅已决定暂不做，待 BYOK 价值验证后再议） | 待评估 |
| **M5 扩展** | 题库组卷、多学科模板、成绩分析 Excel 导入 | 待评估 |

### 里程碑决策门

- M1 → M2：代码完成，需确认 ① 中转站 Key 余额充足 ② 页面体验可接受 ③ 免备案公网部署（海外 VPS + 域名）
- M2 → M3：试用反馈证明「场景价值」后，再做用户体系

### 自媒体获客（小红书/公众号）动作清单（M2）

1. **内容定位**：教师实操干货（“期末评语 3 分钟写完 50 人”“作文批改原来可以这样”）
2. **发布渠道**：小红书图文/视频笔记 + 公众号文章；同步教师社群
3. **产品钩子**：免费试用（填 Key 即用，成本由客户自己承担，转化自然）
4. **转化路径**：笔记 → 落地页（公网部署后的产品链接）→ 填 Key 使用 → 反馈群
5. **素材**：用真实场景输出（批改结果、评语）做对比图“AI 生成 vs 手写”

---

## 11. 风险与开放问题

### 11.1 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| Key 安全（前端→后端透传） | 中 | HTTPS；后端不落盘；二期做服务端加密绑定 |
| 中转站稳定性/上游波动 | 中 | 多模型备用；错误提示友好化 |
| 场景输出质量不及预期 | 中 | 提示词持续迭代；M2 真实教师试用验证 |
| 免费 AI 跟进（如豆包做作文批改） | 低-中 | 靠「场景深度 + 批量 + 拍照」差异化，快速迭代 |
| 教育内容合规（评语/批改尺度） | 低 | 提示词约束（鼓励为主、不贴标签、不施压） |
| 无用户体系导致无法召回 | 中 | 先验证价值；二期补登录 |
| 免备案公网部署的合规与稳定性 | 中 | 海外服务器 + 国际域名；内容合规；详见 11.3 |

### 11.2 开放问题 → 已决策（2026-08-08）

| 问题 | 决策 | 影响 |
|------|------|------|
| 部署形态 | **公网服务器、不备案**（海外服务器 + 国际域名） | 见 11.3；中转站本身已在海外，链路自洽 |
| 模型选型 | **从客户 Key 拉取可用模型列表，由客户自行选择**（`/v1/models` 已验证可行） | 新增「模型选择」功能；见 7.4 补充 |
| 场景订阅 | **暂不做**（M2 先用 BYOK 验证价值） | 里程碑 M4 移除订阅项，改为机构版/白标 |
| 产品名 | **新建，定位「教师自己的 Codex 工作台」** | 见第 13 章候选；待拍板 |
| 推广渠道 | **小红书、公众号等自媒体** | 见 10 里程碑 M2 的获客动作 |
| “一体化工作台”需求 | **不整体集成**（17 个纯 CRUD 模块放弃），只吸收成绩分析 + 评语增强（方案 A） | 见 §5A；避免 3-5 周重复建设 |

### 11.3 免备案公网部署方案

- **服务器**：海外 VPS（新加坡/香港/日本等，延迟低），如 DigitalOcean / Vultr / 阿里云国际 / AWS Lightsail。
- **域名**：国际域名（.com/.cc/.dev 等）接入 Cloudflare（免费 CDN + HTTPS + 防攻击）。
- **备案**：域名指向海外服务器即可免 ICP 备案；不接国内 CDN/OSS 等需备案服务。
- **合规**：内容面向教师工具，无新闻/支付/社区等强监管业务；文案避免敏感词；预留删除/举报机制。
- **链路**：用户 → Cloudflare(HTTPS) → 海外 VPS(Next.js) → api.sublyx.org（已在海外）→ 上游模型。全程海外，不涉及国内节点。
- **Key 安全**：强制 HTTPS（Cloudflare 免费证书），后端不落盘 Key，仅内存透传。

---

## 12. 附录：现有代码资产

项目路径：`D:/pi-agent/teacher-agent/`

```
teacher-agent/
├── README.md                  # 项目说明与快速开始
├── PRD.md                     # 本文档
├── app/
│   ├── page.tsx               # 教师工作台（Pi AgentSession 调试通道）
│   ├── billing-demo/page.tsx  # 自带 Key 页面（生产通道）
│   ├── layout.tsx             # 元数据
│   ├── globals.css            # 样式
│   └── api/
│       ├── llm/route.ts       # 生产调用端点（场景 + Key 透传 + usage）
│       ├── chat/route.ts      # 开发调试端点（Pi AgentSession + 自定义工具）
│       └── artifacts/route.ts # 成果保存/列表/删除
├── lib/
│   ├── scenarios.ts           # 6 个场景提示词（产品壁垒；analysis 待实现）
│   ├── relay-session.ts       # pi SDK 会话封装（生产通道：动态注册 provider + 思考 + 工具 + 图片 + usage）
│   ├── relay.ts               # 中转站直连（地址内置 + 模型列表 + 备用非流式）
│   ├── teacher-data.ts        # 学段/学科/年级/任务模板/课标要点
│   └── teacher-tools.ts       # 自定义 Pi 工具（课标查询/保存成果）
├── artifacts/                 # 教师成果输出目录
│   ├── sample-essay.jpg       # 测试用作文图
│   └── *.md                   # 生成成果示例
└── package.json               # Next.js 16.3 / React 19.2 / pi-coding-agent 0.84
```

**运行**：`npm run dev` → http://127.0.0.1:30300/billing-demo（自带 Key 页）

**方案 A 待实现清单**（见 §5A）：
- [ ] `lib/scenarios.ts` 新增 `analysis` 场景提示词
- [ ] `app/billing-demo/page.tsx` 场景入口加“成绩分析” + 评语表格模板引导
- [ ] 前端成绩文本规范化（粘贴 → 结构）
- [ ] （二期）Excel/CSV 上传解析

---

## 13. 产品名：师座（已定）

> **2026-08-08 拍板**：产品名定为「**师座**」（英文 TeacherDeck）。

### 命名含义

像 Codex 之于程序员一样，师座是**教师专属的 AI 工作台/驾驶舱**：
- 「师」—— 教师
- 「座」—— 驾驶座/工作台/座舱（Codex 工作台式）
- 口号方向：「师座在手，备课批改全都有」「你的 AI 驾驶舱，坐进来就能干活」

### 候选对比（评选过程存档）

| 候选 | 记忆点 | 工具感 | 文化感 | 结论 |
|------|--------|--------|--------|------|
| **师座** | ★★★★★ | ★★★★★ | ★★★ | ✅ 选定 |
| AI 讲台 | ★★★ | ★★★★★ | ★★ | 备选 |
| 杏坛 | ★★★★ | ★★ | ★★★★★ | 备选 |
| 师砚 | ★★★★ | ★★★★ | ★★★★ | 备选 |
| 桃李台 | ★★★★ | ★★★ | ★★★★ | 备选 |

### 域名可用性（2026-08-08 实测）

**已注册（不可用）**：shizuo.ai / .cc / .dev / .com / .cn / .app；teacherdeck.com / .ai

### ✅ 域名已定：teacherdeck.org（2026-08-08 拍板）

> 产品英文名 TeacherDeck → 域名 `teacherdeck.org`（已注册使用）。
> 与品牌一致（TeacherDeck = 教师的 AI 工作台），免备案部署于海外 VPS + Cloudflare。

| 项 | 值 |
|----|-----|
| 正式域名 | **teacherdeck.org** |
| DNS | Cloudflare 托管 |
| 用途 | 师座 Web 产品主入口（https://teacherdeck.org） |

**落点**：`docs/cloudflare-setup.md`（接入步骤）、`deploy/setup.sh`（部署示例）、`README.md`。

> 候选域名记录（备查）：shizuoai.com / shizuoai.cc / getshizuo.com / shizuo.work / shizuo.tech 等曾为可用备选，已弃用。

---

*本文档为交流过程沉淀稿，欢迎评审后修订。*
